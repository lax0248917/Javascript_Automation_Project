// App to run to follow all suggested hashtags in LinkedIn

const followingBtns = document.querySelectorAll('.full-width'); // this selector selected all the buttons on the page, so it is not the best solution.

followingBtns.forEach(followbtn => followbtn.click());

